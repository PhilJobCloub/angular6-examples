import { slideIn } from './slide-in.animations';
import { fadeOut } from './fade-out.animations';

export const animation : any[] = [
    slideIn,
    fadeOut
  ];

export { slideIn } from './slide-in.animations';
export { fadeOut } from './fade-out.animations';
